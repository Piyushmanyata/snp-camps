{
  "_comment": "Per-route gzipped EAGER/initial client JS budgets (bytes). #71: budgets measure only sync-reachable production chunks; async islands (jsqr, scanner shell, optional admin) excluded. Ratcheted 2026-07-26 from measured post-fix baseline (budgetFromMeasured ≈ ceil(gzip*1.03/1000)*1000). Never raise to hide a regression. 2026-07-28 reconciliation: /lookup added (route shipped by #114 with no entry, so the gate could not pass); /s/[token] 199000→206000 for the patient QR added to the extended status page (#114); /self-register 205000→220000 for the Aadhaar scanner island and receipt QR (#113) — the decoder itself stays deferred.",
  "measureMode": "eager-initial-vs-async-deferred",
  "routes": {
    "/": 209000,
    "/_global-error": 193000,
    "/_not-found": 199000,
    "/admin": 419000,
    "/admin/patients": 406000,
    "/counter": 418000,
    "/doctor": 412000,
    "/login": 273000,
    "/lookup": 206000,
    "/p/[id]": 199000,
    "/print/[id]": 212000,
    "/print/prescription/[id]": 208000,
    "/register": 411000,
    "/self-register": 220000,
    "/s/[token]": 206000,
    "/volunteer": 418000
  }
}
