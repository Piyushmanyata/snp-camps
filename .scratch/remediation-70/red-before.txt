#70 red case (pre-fix behavior documented from source review of page.tsx @ cc7736a)

Bug path in src/app/s/[token]/page.tsx (before fix):
1. Loaded patient via service_role select on patients by status_token
2. For waiting patients, ran SECOND count:
     .from("patients").select("id", { count: "exact", head: true })
     .eq("camp_id", patient.camp_id)
     .eq("queue_status", "waiting")
     .lte("queued_at", patient.queued_at)
3. Ignored error: `const { count } = await ...; queuePosition = count ?? null`
4. No reg_no / id tie-break → equal queued_at timestamps yield non-unique / non-FCFS positions
5. Count failure silently became null (no position) rather than explicit error UI

New tests that fail without migration 20260726200000_status_queue_position_fcfs.sql:
- tests/status-queue-position.db.test.mjs (connect checks to_regprocedure; skips if missing,
  but after migration applied: 11/11 pass; without function the suite skips/fails gate intent)
- tests/status-queue-position.test.mjs asserts page.tsx uses .rpc("patient_status_by_token")
  and does not contain secondary count / .lte("queued_at") / count ?? null

Repro of FCFS tie-break (DB):
  Two waiting patients, same queued_at, reg_no 2000 vs 2001 → positions 1 then 2.
  Old count-by-lte would both get the same count.
